using NUnit.Framework;
using System;
using System.Collections.Generic;
using System.Text;

namespace CryptLink.SigningFrameworkTests
{
    [TestFixture]
    class ConsistantHashTests
    {
        [Test]
        public void SigningTests() {
            throw new NotImplementedException("Test preformance here");
            // Assert.AreEqual(hash1.Rehash().Bytes, hash2.Rehash().Bytes, "Rehash of the same strings returns true for provider: '" + provider.ToString() + "'");

        }

    }
}
